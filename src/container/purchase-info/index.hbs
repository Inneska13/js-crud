{{! 
Це код container файлу для index сторінки, 
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
 }}
<main class="main">

  {{#with data}}
    <form method="POST" action="/purchase-edit?id={{id}}" class="form-card">

     {{> heading title="Інформація про замовлення"}}

     {{> divider}}

	 {{> field label="Iм'я" name="firstname"}}
	 {{> field label="Прізвище" name="lastname"}}
	 {{> field label="Телефон" name="phone"}}
	 {{> field label="Пошта" name="email"}}
	 {{> field label="Коментар" name="comment"}}
	 {{> field label="Списати бонуси" name="bonus"}}
	 {{> field label="Промо-код" name="promocode"}}

	 {{> divider}}

	 {{> field label="Сума до сплати" value=totalPrice name="totalPrice" }}
	 {{> field label="Вартість товарів" value=productPrice name="productPrice" }}
     {{> field  label="Вартість доставки" value=deliveryPrice name="deliveryPrice" }}
     {{> field label="Нараховані бонуси" value=bonus name="bonus" }}


	 {{> button text="Змінити дані"}}

    </form>
  {{/with}}

</main>

{{! 
    Цей код містить лише те, що буде виводитись в <body>
    Адже наш layout/default.hbs має вже структуру HTML
}}